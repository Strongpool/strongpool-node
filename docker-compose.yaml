version: "3.0"
services:
  # TODO set ulimit
  arweave:
    image: "arweave:latest"
    restart: on-failure
    ports:
      - "1984:1984"
    volumes:
      - type: bind
        source: ./data
        target: /data
    environment:
      - ARWEAVE_ARGS
