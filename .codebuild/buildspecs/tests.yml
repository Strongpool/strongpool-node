---

version: 0.2

env:
  shell: bash

phases:
  pre_build:
    commands:
      - dockerd-entrypoint.sh &
      - until docker info >/dev/null 2>&1; do sleep 1; done

  build:
    commands:
      - ./bin/lint
      - ./bin/rcf-tests

cache:
  paths:
    - '/root/.m2/**/*'
