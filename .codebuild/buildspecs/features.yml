---

version: 0.2

phases:
  pre_build:
    commands:
      - dockerd-entrypoint.sh &
      - until docker info >/dev/null 2>&1; do sleep 1; done

  build:
    commands:
      - ./.codebuild/features-build

cache:
  paths:
    - '/root/.m2/**/*'
