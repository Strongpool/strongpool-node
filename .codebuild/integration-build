#!/usr/bin/env bash

if git log --pretty=full -1 | grep 'skip ci'; then
  exit 0
fi

echo "ARWEAVE_IMAGE_TAG=$ARWEAVE_IMAGE_TAG" > .env
git add .env
git commit -m $"Update service container image tags\n\nskip ci"
git push
